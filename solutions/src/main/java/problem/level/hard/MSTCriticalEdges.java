package problem.level.hard;

public class MSTCriticalEdges {

    //find both critical edges and non-critical edges that builds up all possible MSTs
    // a critical edge exists in every possible MST
}
